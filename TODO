Containers should handle listeners

Create functions to remove listener functions

All components should have events passed, can override as they like

Implement gridPanes

Implement textboxes

Allow panels background images. Render image atop colour
Allow panels to have minimum sizes (returned if minimum size of children smaller)

Create basic shaders to render solid coloured boxes and textured boxes (Should include clip space)

Remove all using namespace stuff in headers (and maybe all files)

Remove using namespace glm from all files.
Remove using namespace from all header files.